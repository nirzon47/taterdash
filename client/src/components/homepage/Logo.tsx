const Logo = () => {
	return (
		<h2
			className='text-3xl italic font-archivo font-black cursor-pointer'
			onClick={() => (window.location.href = '/')}
		>
			taterDash
		</h2>
	)
}

export default Logo
